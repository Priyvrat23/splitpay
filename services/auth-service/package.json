{
  "name": "auth-service",
  "version": "1.0.0",
  "description": "SplitPay Auth Service — handles registration, login, and JWT tokens",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  }
}
